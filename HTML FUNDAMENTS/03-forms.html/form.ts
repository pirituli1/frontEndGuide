interface registroEvent{
    name: string,
    email: string,
    password: string,
    ticketType: 'general' | 'vip' | 'student'
}

const main = ():Object => {

    const getData = async <registroEvent>() => {
        return {
            name: document.getElementById('')
        }
    }

    return {}
}